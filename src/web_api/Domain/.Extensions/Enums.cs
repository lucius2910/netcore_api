namespace Domain.Enums
{
    public enum Gender
    {
        Male = 0,
        FeMale = 1,
        Other = 2
    }

    public enum CalendarStatus
    {
        FullDay = 0,
        PartDay = 1,
        Absent = 2
    }

    public enum ReceiveOrderStatus
    {
        UnConfirmed = 0,
        Confirmed = 1
    }
}
